name: CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

  workflow_dispatch:

jobs:
  verification:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run all tests
        run: ./gradlew test
